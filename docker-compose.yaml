version: '3.8'

services:
  # Main FastAPI application
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    container_name: v0agent_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data                    # Persistent data
      - ./uploads:/app/uploads              # User uploads
      - ./logs:/app/logs                    # Application logs
      - ./.env:/app/.env:ro                 # Environment file (read-only)
    environment:
      - DATABASE_PATH=/app/data/agent.db
      - UPLOAD_DIR=/app/uploads
      - LOG_DIR=/app/logs
      - LOG_LEVEL=INFO
      # Redis for caching
      - REDIS_URL=redis://redis:6379/0
      # Embeddings via Supabase pgvector
      - EMBEDDINGS_PROVIDER=supabase
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - v0agent_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
  
  # Redis for caching
  redis:
    image: redis:7-alpine
    container_name: v0agent_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - v0agent_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3

  # pgAdmin for database management (dev only - use: docker-compose --profile dev-tools up)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: v0agent_pgadmin
    ports:
      - "5050:80"
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@example.com
      - PGADMIN_DEFAULT_PASSWORD=admin
    networks:
      - v0agent_network
    profiles:
      - dev-tools

volumes:
  redis_data:

networks:
  v0agent_network:
    driver: bridge
